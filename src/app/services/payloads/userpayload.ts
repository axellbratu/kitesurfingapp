//payload declarations for data type management

export interface Userpayload {
    email: String,
    avatar: string,
    name: string,
    createdAt: Date,
    id: number
}

export interface loginIds{
    id:number,
    userId:number
}

